// Shared Module
// Master index for all shared resources
// Note: Import directly from specific folders to avoid export conflicts

// Available shared resources:
// - @/shared/ui-components  (Toast, ConfirmModal, LoadingSpinner, etc.)
// - @/shared/layouts        (Header, Sidebar, NavigationHeader, etc.)
// - @/shared/providers      (AuthProvider, ThemeProvider, ProtectedRoute, etc.)
// - @/shared/api            (api client)
// - @/shared/auth           (useAuthStore, useDashboardStore)
// - @/shared/hooks          (useApi, usePermission, useLocalStorage, etc.)
// - @/shared/utils          (logger, validators, formatters, etc.)
// - @/shared/types          (api.types, ipr.types, research.types, etc.)
// - @/shared/constants      (app constants)
// - @/shared/services       (auth, audit, notification, profile services)
